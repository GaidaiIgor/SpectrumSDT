#ifndef template_funcs
#define template_funcs

#define CONCAT(a, b, c) a ## b ## c
#define CONCAT2(a, b) CONCAT(a, b,)
#define CONCAT3(a, b, c) CONCAT(a, b, c) /* secondary call is necessary to expand macro arguments */

#endif
